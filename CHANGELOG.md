# Changelog
جميع التغييرات الملحوظة في Ultra Apex مُوثّقة هنا.

## [4.4.0] - 2026-05-05
### 🔧 Fixed
- `refill()`: رفع `BUF_SIZE` إلى `512` (`GLOBAL_BATCH`) لمنع استدعاءات `grow()` المتكررة وتضخم الذاكرة.
- `refill()`: إصلاح منطق إعادة الفائض، مما أعاد كفاءة إعادة استخدام الذاكرة بعد التجزؤ.
- إزالة `pop_all_unpacked` المعطوب؛ استبداله بحلقة فك سلسلة آمنة محلياً.
- تبسيط سياسة `maybe_flush_to_global()` لإدارة أكثر كفاءة للمستودع.

### ⚡ Enhanced
- `grow()` يعطي أولوية للمستودع المحلي (16 كتلة) قبل الدفع للكومة العالمية بدفعات `128`.
- الاحتفاظ بـ `prepare_spare()` بدون قفل عبر `atomic CAS` (من v4.3).
- ضمان محاذاة الكتل `alignof(max_align_t)` وصفر عمليات ذرية على المسار الساخن.

## [4.3.0]
### Added
- تحضير مسبق للكتل بدون قفل (`CAS-only`).
- استخدام `pop_all()` في مسار `refill` لتقليل العمليات الذرية من `O(n)` إلى `O(1)`.

### ⚠️ Known Issues (تم علاجها في v4.4)
- `BUF_SIZE=32` تسبب في استهلاك ذاكرة أكبر بـ `4.2×`.
- تدهور أداء إعادة الاستخدام بعد التجزؤ بـ `~17×`.
- سياسة تفريغ المستودع المعقدة أعاقت الكفاءة.

## [4.2.0]
- البنية الأساسية: مجلة محلية + مستودع لكل CPU + كومة عالمية.
- Steady-State RT: `~20 ns/op` (64B).
